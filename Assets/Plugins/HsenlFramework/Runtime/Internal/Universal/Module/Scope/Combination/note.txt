// 还需要继续完善组合条件, 以及更灵活的跨域匹配的规则, 方便定义自己的匹配策略
//      能够指定匹配, 而不只是限于同域? 但这样会打破我们以scope为核心的思想, 让匹配情况变的混乱
//      跨域匹配的必要? 有没有其他更好的方案
//      能否不用继承实现, 这样框架内置的组件也可以不继承unbodied了
//      不自动require scope组件, 允许不使用该系统
//      