namespace Hsenl {
    public enum PliRecoverPriority {
        /// <summary>
        /// 增加数值
        /// </summary>
        AddHpNum,
        
        // view表现(音效、特效等)
        ViewExpression,
    }
}